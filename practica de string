#include "stdafx.h"
#include <iostream>
#include "conio.h"
#include <string>

using namespace std;

void leer(string nombre);
int longituds(string nombre);
void compare(string nombre);
void elementos_individuales(string nombre);
void elementos_individuales_at(string nombre);
void sacar_parte_string(string nombre);
void intercambia(string nombre);
void encuentra_caracteres(string nombre);
void invertir_frase(string nombre);
void quitar_vocales(string nombre);

void main()
{
	int longitud,cont,pos,op;
	char aux;
	
	string var1, var2, nombre; //Declarar strings

	do{
		cout<<"      MENU"<<endl;
		cout<<"(1) leer datos string"<<endl;
		cout<<"(2) uso de length para determinar la longitud de un string"<<endl;
		cout<<"(3) uso de compare para determinar si dos cadenas son iguales"<<endl;
		cout<<"(4) uso de elementos individuales (Numero de vocales en el nombre)"<<endl;
		cout<<"(5) uso de elementos individuales con at (Numero de vocales en el nombre)"<<endl;
		cout<<"(6) Sacar una parte del string"<<endl;
		cout<<"(7) Intercambia string"<<endl;
		cout<<"(8) Encuentra caracteres en string"<<endl;
		cout<<"(9) Invertir una frase "<<endl;
		cout<<"(10) Quitar las vocales"<<endl;
		cout<<"(0) SALIR"<<endl;
		cout<<endl;
		cout<<"ingrese una opcion"<<endl;
		cin>>op;

		switch(op)
		{case 1:	
			{leer(nombre);
			break;}
		case 2:
			{int longitud;
			leer(nombre);
			longitud=longituds(nombre);
			cout<<"la longitud es:"<<longitud<<endl;
			break;}
		case 3:
			{leer(nombre);
			compare(nombre);
			break;}
		case 4:
			{leer(nombre);
			elementos_individuales(nombre);
			break;}
		case 5:
			{leer(nombre);
			elementos_individuales_at(nombre);
			break;}
		case 6:
			{leer(nombre);
			sacar_parte_string(nombre);
			break;}
		case 7:
			{leer(nombre);
			intercambia(nombre);
			break;}
		case 8:
			{leer(nombre);
			encuentra_caracteres(nombre);
			break;}
		case 9:
			{leer(nombre);
			invertir_frase(nombre);
			break;}
		case 10:
			{leer(nombre);
			quitar_vocales(nombre);
			break;}
		case 0:
			{cout<<"SALIR"<<endl;
			break;}
		default:
			{cout<<"NO ES UNA OPCION"<<endl;
			break;}
		}
	}while(op!=0);
	getch();
}

void leer(string nombre)
{
	cin.ignore();
	cout<<endl<<"Ingrese un nombre: ";
	getline(cin,nombre);
	
}
int longituds(string nombre)
{
	int longitud;
	longitud=nombre.length();
	return(longitud);
}

void compare(string nombre)
{	string var2;
	cin.ignore();
	cout<<endl<<"Redigite el nombre: ";
	getline(cin,var2);

		if (nombre.compare(var2) == 0 )
		cout<<endl<<"Los datos coinciden"<<endl;
	else 
		cout << nombre << " es diferente que "<<var2 << " :1"<<endl;
}
void elementos_individuales(string nombre)
{	
	int cont, longitud;
	longitud=longituds(nombre);
	cont=0;
	cout<<endl<<"Ejemplo de uso de elementos individuales: "<<endl;
	cout<<endl<<"Contar vocales"<<endl<<endl;
	for (int i=0;i<longitud;i++)
		if (nombre[i]=='a' || nombre[i]=='e'||nombre[i]=='i'||nombre[i]=='o'||nombre[i]=='u')
			cont++;
	cout<<"Numero de vocales: "<<cont<<endl;
}

void elementos_individuales_at(string nombre)
{
	int cont,longitud;
	longitud=longituds(nombre);
	cout<<endl<<"Ejemplo de uso de elementos individuales con at: "<<endl;
	cont=0;
	for (int i=0;i<longitud;i++)
		if (nombre.at(i)=='a' || nombre.at(i)=='e'||nombre.at(i)=='i'||nombre.at(i)=='o'||nombre.at(i)=='u')
			cont++;
	cout<<"Numero de vocales: "<<cont<<endl;
}

void sacar_parte_string(string nombre)
{	int pos, longitud;
	longitud=longituds(nombre);
	cout<<endl<<"Ejemplo de uso de substr"<<endl;
	cout<<endl<<"Ingrese desde donde desea extraer el sbstring: ";
	cin>>pos;
	cout <<nombre.substr(pos,longitud-pos);
	cin.ignore();
}

void intercambia(string nombre)
{	string var2;
	leer(var2);
	cout<<endl <<"Intercambia variables";
	nombre.swap(var2);
	cout << nombre;
	cout << var2;
	
}
void encuentra_caracteres(string nombre)
{
	int longitud;
	string var2;
	leer(var2);
	cout << var2.find(' ');
	longitud=var2.length();
	for (int i=0;i<longitud;i++)
		if (var2.at(i)==' ' )
			cout << endl<<"Existe un espacio en: "<<i;
}
void invertir_frase(string nombre)
{
	string var2;
	int longitud;
	char aux;
	var2=nombre;
	longitud=nombre.length();
	for (int i=0;i<longitud/2;i++)
	{
		aux=nombre[i];
	    nombre[i]=nombre[longitud-1-i];
		nombre[longitud-1-i]=aux;
	}
	cout <<"La cadena invertida es: "<< nombre<<endl;
	
	if (var2.compare(nombre)==0)
		cout<<"Es un palindromo";
}
void quitar_vocales(string nombre)
{
	string var2;
	int longitud, cont;
	var2=nombre;
	cont=0;
	longitud=nombre.length();
	for (int i=0;i<longitud;i++)
		if (nombre[i]!='a' && nombre[i]!='e'&& nombre[i]!='i'&& nombre[i]!='o'&& nombre[i]!='u')
		{
			var2[cont]=nombre[i];
			cont++;
		}
	cout<<"Sin vocales es: "<<var2;
}
